spring:
  application:
    name: moinProject
  h2:
    console:
      enabled: true
      path: /h2-console
  datasource:
    url: jdbc:h2:mem:testdb;NON_KEYWORDS=USER
    driverClassName: org.h2.Driver
    username: sa
    password: ''
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        default_batch_fetch_size: 1000

server:
  port: 8083

encryption:
  key: testmointestmoin

jwt:
  secret: kukaedenkukaedenkukaedenkukaedenkukaedenkukaedenkukaedenkukaedenkukaedenkukaeden

